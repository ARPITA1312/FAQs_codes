#include <bits/stdc++.h>
using namespace std;
string removeVowel(string str){
    regex r("[aeiouAEIOU]");
    return regex_replace(str, r, "");
}
int main(){
    string str = "Geeks";
    cout<<(removeVowel(str));
    return 0;
};
